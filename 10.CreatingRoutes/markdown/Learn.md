//part-3
for routing we have to install a js librabry and that lib is react router dom 
```
npm i react-router-dom
```

now let make  about us page or about component 



pat 4 ending 
two types routing 
server side routing  -> click reload whole page fetch html and render it to page 

client side routing -> all the component present at one route 
we are using client side routing here